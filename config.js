/* ============================================================
   Chem Battle — 접속 설정
   ------------------------------------------------------------
   Supabase 대시보드 → Settings → API 에서 두 값을 복사해
   아래에 붙여넣으세요.

     Project URL      →  SUPABASE_URL
     anon public key  →  SUPABASE_ANON_KEY

   비워두면 예전처럼 이 브라우저에만 저장되는 연습 모드로
   동작합니다. 실제 수업에서는 반드시 채워야 합니다.

   주의: anon key 는 공개되는 값입니다. 학생 브라우저에서
   보입니다. 그러니 이 앱에 성적이나 개인정보를 넣지 마세요.
   PIN 도 다른 곳에서 쓰는 비밀번호를 재사용하지 않도록
   학생들에게 안내해 주세요.
   ============================================================ */

window.CHEM_CONFIG = {
  SUPABASE_URL: 'https://ruhkkivsmxqopmrqfpci.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_HkmSInXrc9-BSZHyiArp8Q_hHImdIAY',

  // 교수 계정은 'Professor' 라는 닉네임으로 '캐릭터 만들기'를 한 번 하면
  // 만들어집니다. 관리자는 최초 1명만 생성되도록 서버가 막습니다.
};
