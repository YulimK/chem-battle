# Chem Battle — 실제 수업용 설정

라이브 퀴즈와 채팅이 학생들 사이에서 실제로 동작하려면 공용 데이터베이스가
필요합니다. Supabase 무료 등급으로 충분합니다. 30분이면 끝납니다.

---

## 1. Supabase 프로젝트 만들기

1. https://supabase.com 에서 GitHub 계정 등으로 가입
2. **New project** 클릭
3. 이름은 `chem-battle`, 리전은 **Northeast Asia (Seoul)** 선택
4. Database Password 는 아무거나 정하고 따로 적어두세요 (앱에서는 안 씁니다)
5. 프로젝트 생성까지 1~2분 기다립니다

## 2. 테이블 만들기

1. 왼쪽 메뉴 **SQL Editor** → **New query**
2. `supabase-schema-secure.sql` 내용을 통째로 붙여넣고 **Run**
3. 새 쿼리를 열어 `supabase-prices.sql` 도 붙여넣고 **Run**
   (상점 2,599개의 가격표입니다. 서버가 가격을 알아야 구매 금액을
   검증할 수 있습니다. 조금 오래 걸릴 수 있어요)
4. 둘 다 "Success" 가 뜨면 완료입니다

## 3. 접속 정보 넣기

Supabase 대시보드 구성이 바뀌어서 메뉴 이름이 예전 안내와 다릅니다.
아래 세 가지 중 편한 방법을 쓰세요.

### 방법 A — Connect 버튼 (가장 빠름)

프로젝트 화면 위쪽의 **Connect** 버튼을 누르면 대화상자가 열립니다.
**App Frameworks** 탭에 Project URL 과 키가 함께 나옵니다.

### 방법 B — 주소창에서 URL 만들기

지금 보고 있는 브라우저 주소가 이렇게 생겼습니다.

```
https://supabase.com/dashboard/project/abcdefghijklmnop
```

맨 뒤의 문자열이 프로젝트 ID 입니다. 이걸로 URL 을 조합합니다.

```
https://abcdefghijklmnop.supabase.co
```

### 방법 C — Settings → API Keys

왼쪽 맨 아래 톱니바퀴(**Settings**) → **API Keys** 메뉴입니다.
예전에는 `API` 였는데 이름이 바뀌었습니다.

- `sb_publishable_...` 로 시작하는 키가 보이면 그걸 쓰세요.
- 없으면 **Legacy API Keys** 탭의 **anon public** 키를 쓰세요.
  `eyJhbGci...` 로 시작하는 긴 문자열입니다.

둘 중 어느 쪽이든 이 앱에서 그대로 동작합니다.

> **절대 넣으면 안 되는 키**
> `service_role` 또는 `sb_secret_` 으로 시작하는 키는 모든 보안 정책을
> 우회하는 마스터 키입니다. 학생 브라우저에 노출되면 데이터베이스가
> 통째로 열립니다. `config.js` 에는 절대 넣지 마세요.

### 붙여넣기

`config.js` 를 열어 두 값을 채웁니다.

```js
window.CHEM_CONFIG = {
  SUPABASE_URL: 'https://abcdefghijklmnop.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_...',   // 또는 eyJhbGci...
};
```

## 4. 확인

앱을 열면 상단 제목 옆에 작은 점이 보입니다.

| 점 색깔 | 의미 |
|---|---|
| 🟢 초록 | 수업 서버 연결됨 — 정상 |
| 🔴 빨강 | 연결 실패 — 주소나 키를 확인하세요 |
| ⚫ 회색 | 연습 모드 — `config.js` 가 비어 있음 |

교수 계정으로 로그인하면 대시보드 맨 위에 연결 상태 문구도 나옵니다.

---

## 수업 당일 순서

1. 교수님이 닉네임 `Professor` 로 **캐릭터 만들기** 를 한 번 진행합니다.
   이때 정한 PIN 이 관리자 비밀번호가 되니 잘 기억하세요.
   관리자는 최초 1명만 만들어지도록 서버가 막으므로, 학생이 나중에
   같은 닉네임을 시도해도 관리자가 되지 않습니다.
2. Live Quiz 문제를 입력하고 **SAVE**
3. 학생들에게 주소를 공유하고 각자 캐릭터를 만들게 합니다
4. 수업 중 **START** → 학생 화면에 즉시 문제가 뜹니다
5. 학생이 제출 → 대시보드의 응답 수가 실시간으로 올라갑니다
6. **END** → 학생 화면에 정답과 응답 분포가 표시되고 포션이 지급됩니다
7. 다음 문제는 **START NEXT QUIZ**

---

## 알아두실 점

**보안 수준** — 클라이언트는 테이블에 직접 쓰지 못하고, 모든 변경이
서버 함수를 거칩니다. 그 결과 다음이 막힙니다.

- PIN 은 bcrypt 로 해시되어 저장됩니다. 데이터베이스를 통째로 봐도
  원래 PIN 을 알 수 없습니다.
- 포션과 경험치는 서버가 계산합니다. 브라우저에서 숫자를 바꿔 보내도
  반영되지 않습니다.
- 상점 가격을 서버가 다시 계산합니다. 0원으로 사겠다고 보내도 거절됩니다.
- 라이브 퀴즈 정답은 교수가 END 를 누르기 전까지 서버 밖으로 나가지
  않습니다. 개발자도구로도 미리 볼 수 없습니다.
- 주차별 퀴즈는 서버가 채점합니다. 정답과 해설은 제출 후에만 옵니다.
- 채팅 이름은 로그인 토큰에서 꺼내므로 남의 이름을 사칭할 수 없습니다.
  10초에 5회 이상 보내면 차단됩니다.
- 퀴즈 시작·종료, 문제 편집은 교수 계정 토큰이 있어야만 실행됩니다.

**그래도 남는 한계** — 자기 계정으로 로그인한 뒤 라이브 퀴즈에 답을
제출하는 것 자체는 정상 동작이므로, 친구에게 답을 알려주는 식의 부정은
기술로 막을 수 없습니다. 또한 이 앱은 개인정보를 담지 않도록 설계했으니
학번이나 실명을 닉네임으로 쓰지 않게 안내해 주세요. PIN 도 다른 서비스
비밀번호를 재사용하지 말라고 일러두시면 좋습니다.

**수강 인원 한계** — 무료 등급의 실질적인 상한은 **동시 접속 200명**입니다.
한 강의실에서 동시에 앱을 켜는 인원이 200명을 넘지 않으면 됩니다.
수강생이 200명을 넘어도 분반이 나뉘어 동시 접속이 200명 이하라면 괜찮습니다.

실시간 메시지는 월 200만 건인데, 100명 기준 한 학기 전체가 약 7만 건으로
한도의 4% 수준입니다. 200명이어도 7% 정도라 넉넉합니다.

**일시 정지 주의** — 무료 프로젝트는 일주일 동안 접속이 없으면 자동으로
멈춥니다. 방학이나 시험 기간에 공백이 생기면, 수업 전날 대시보드에 한 번
들어가 재개(Restore)해 두세요.

**응답 집계** — 학생 한 명당 문제 하나에 한 번만 답할 수 있습니다.
다른 기기에서 다시 접속해도 이미 답한 문제는 대기 화면이 나옵니다.

**연습 모드** — `config.js` 를 비워두면 예전처럼 브라우저에만 저장됩니다.
디자인을 수정하며 테스트할 때 유용합니다.

---

## 문제가 생기면

**빨간 점이 뜬다** — `config.js` 의 URL 과 키에 따옴표가 빠지거나 공백이
섞이지 않았는지 확인하세요. 브라우저 개발자도구(F12) 콘솔에 원인이 찍힙니다.

**학생 화면이 안 바뀐다** — SQL 을 실행할 때 마지막 "실시간 전송 켜기"
부분까지 포함됐는지 확인하세요. Supabase 대시보드의 Database → Replication
에서 `room`, `responses`, `messages` 가 켜져 있어야 합니다.

**로그인이 안 된다** — Table Editor 에서 `users` 테이블을 열어 해당
닉네임이 있는지 보세요. 없다면 캐릭터 만들기부터 다시 해야 합니다.

**`function gen_salt(unknown) does not exist`** — Supabase 는 pgcrypto 를
`extensions` 스키마에 설치하는데, 함수가 `public` 만 보고 있어서 생기는
오류입니다. `supabase-fix-pgcrypto.sql` 을 SQL Editor 에 붙여넣고 Run 하면
해결됩니다. (최신 `supabase-schema-secure.sql` 에는 이미 반영돼 있습니다.)
